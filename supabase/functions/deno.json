{
  "//": "Matches the house style the Dart side uses: single quotes, and lines wide enough for the comments to be sentences. Without this, `deno fmt` would rewrite every file in this directory to its own defaults.",
  "fmt": {
    "singleQuote": true,
    "lineWidth": 90,
    "exclude": ["deno.lock"]
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
